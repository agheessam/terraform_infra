
#DB connection and queries

7.	Connect to the above-created database from the private instance (Private1).

mysql -h your-db-endpoint -u admin -p


8.	Create a table in the database (Name: AWS) with 2 fields: Name (VARCHAR) and ID (INT).

create table AWS (

Name (VARCHAR),
ID int not null auto_increment,
primary key (ID)
);

9.	Insert 2 rows of data into the created table AWS with values for the fields Name and ID.

insert into AWS (ID,Name) values (1,Anthony);
insert into AWS (ID,Name) values (2,Samuel);

10.	Show the inserted data in the table by running the query from the private instance (Private1) only.

select * from aws;